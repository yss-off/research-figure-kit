# 2026-09-21：掩码分辨率机制图复盘

## 范围与证据

依据本次论文讨论中的用户纠正，以及已接受图的 manifest、规范源和渲染辅助脚本复盘。原工程证据位于 `semantic_spectrum_sensing/outputs/paper/twc_logic_v0.1_20260921/figures/`；本记录不复制未发表论文资产到公开插件中。证据路径仅供维护核查，运行时 skill 不依赖该工程。

用户最终接受了删去底部长篇说明、直接在两条架构路径中嵌入 LoRa 扫频抽象网格的版本。接受的是逻辑初稿示意图，不是实测预测图或投稿终稿。

## 问题与此次改进

| 实际问题 | 原因与此次处理 |
| --- | --- |
| 架构图与独立的粗细网格面板看不出关联 | 在机制比较规范中要求样例与生成它的阶段直接关联，让操作、表征和可能后果可追踪 |
| 原版已有多尺度融合，展开我们的融合支路容易被读成新增贡献 | 比较前区分原版、简化本地基线和当前实现；同等抽象层次呈现共有机制，高亮只对应已支持的改动 |
| 横向直线没有展示扫频轮廓表达差异 | 选择能暴露限制的领域相关形状；上下使用同一参考轮廓、范围和方向，不把示意栅格当实测分割 |
| 底部长篇解释挤占主图 | 就地短标签解释机制；长论证移到图注或正文；用户删掉的说明不再自动塞回图内 |
| 最终图需多次导出，局部修改容易扩大范围 | 复用规范源、稳定 ID 与已有渲染步骤；局部删除保持其余节点、连接不变，重查裁剪后导出 |
| 网格被当作大量流程节点，参考斜线被当作流程连线 | 规范中区分语义模块与绘图原语；底层能力问题单独记录，不在 overlay 复制补丁 |

已更新论文 overlay 的 SKILL.md 入口与 semantic-and-layout-gates.md，未改 base、版本、安装缓存或论文原始图。工程已有的海报与版本调整保持原样。

## 底层问题：维护记录，未修复

工程 AGENTS.md 规定 bundled drawio 仅通过固定上游 rebase 更新。以下不在此次 overlay 中修补，也不声称升级已解决：

1. **参考斜线表达不顺畅（源码与产物确认）**：base `spec.schema.json` 的 routing 仅有 orthogonal/rounded，`generateConnectorStyle` 默认生成 orthogonalEdgeStyle。本图的两个无箭头参考轨迹由辅助脚本移除锚点和正交路由参数，再设 edgeStyle=none。后续上游应评估独立的 reference-stroke/primitive 表达，避免将信号轨迹伪装为流程边。
2. **装饰网格的校验噪声（历史产物记录）**：manifest 记载网格装饰引起 label-fit/complexity 提示；辅助脚本清空不可见标签并设方角。此次未重新运行原始版本复现全部告警，不能断言每条都是校验器缺陷。上游候选验收应区分装饰原语与语义节点，并保留真实流程边的碰撞检查。
3. **重绘可复现性依赖附加步骤（产物确认）**：YAML 到最终图还有独立后处理，单独重跑 YAML 不足以重建已接受图。现有 bundle 已保存这些步骤；上游改进应让标准源表达这些细节，而不是增加更多临时脚本。

建议的上游回归场景：两个相同范围的粗细网格、同一条无箭头斜参考线、正常流程箭头；验证轨迹保持直线、网格不触发文本溢出、流程箭头校验仍生效。未在本次执行上游修复或外部提 issue。

## 验证

- `quick_validate.py`：论文绘图 skill 格式检查通过。
- `make test`：海报 6 项、论文 overlay 10 项、科研数据图 6 项，共 22 项通过，base CLI 入口检查通过。
- `git diff --check`：通过；base 目录无改动。
- 人工复核：新增规则可从 SKILL.md 定位，未引入本地论文资产依赖或新的安装/审批流程。

指令与 smoke 校验不能证明下一次模型绘图一定一次成功；本侧线程未进行独立代理绘图测试。未提交、发布或同步安装缓存。
